{
  "name": "@browserql/client",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "source": "src/index.ts",
  "license": "ISC",
  "keywords": [
    "browserql",
    "graphql",
    "apollo",
    "browser"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/pierrot.le.foo/browserql.git"
  },
  "bugs": {
    "url": "https://github.com/pierrot.le.foo/browserql/issues"
  },
  "scripts": {
    "build": "rm -rf dist && yarn tsc",
    "watch": "yarn build -w",
    "test": "jest dist",
    "prepublishOnly": "git tag | xargs git tag -d && yarn build && yarn test"
  },
  "dependencies": {
    "@apollo/client": "^3.3.12",
    "@graphql-tools/schema": "^7.1.3",
    "apollo-cache-inmemory": "^1.6.6",
    "apollo-link": "^1.2.14",
    "apollo-utilities": "^1.3.4",
    "graphql": "^15.5.0",
    "iterall": "^1.3.0"
  }
}
